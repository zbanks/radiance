/* CFGSECTION(name, ...) */
/* CFG(name, function, default) */
CFGSECTION(ui,
    CFG(enabled, INT, 1)
)

CFGSECTION(path,
    CFG(layout, STRING, "conf/layout.ini")
    CFG(midi, STRING, "conf/midi.ini")
    CFG(images, STRING, "assets/images/")
    CFG(fonts, STRING, "assets/fonts/")
)

CFGSECTION(state,
    CFG(n_states, INT, 4)
    CFG(path_format, STRING, "conf/state/state_%d.ini")
)

CFGSECTION(output,
    CFG(max_energy, FLOAT, 3.00)
    CFG(gamma, FLOAT, 1.0)
)

CFGSECTION(render,
    CFG(alpha_gamma, FLOAT, 1.0)
)

CFGSECTION(flux,
    CFG(enabled, INT, 1)
    CFG(verbose, INT, 0)
    CFG(timeout, INT, 100)
    CFG(broker, STRING, "tcp://localhost:1365")
    //CFG(broker, STRING, "tcp://musicazoo.mit.edu:1365")
)

CFGSECTION(lux,
    CFG(enabled, INT, 0)
    //CFG(verbose, INT, 0)
    CFG(timeout, INT, 1000)
    CFG(address, STRING, "127.0.0.1")
    CFG(port, INT, 1375)
)

CFGSECTION(timebase,
    CFG(antidelay_ms, INT, 5)
    CFG(ui_bpm_alpha, FLOAT, 0.9)
    CFG(beat_click_alpha, FLOAT, 0.4)
    CFG(beat_btrack_alpha, FLOAT, 0.95)
)

CFGSECTION(audio,
    CFG(use_portaudio, INT, 1)
    CFG(use_jack, INT, 1)
    CFG(sample_rate, INT, 44100)
    CFG(chunk_size, INT, 128)
)

#undef CFGSECTION
#undef CFGSECTION_LIST
#undef CFG
#undef CFG_LIST
